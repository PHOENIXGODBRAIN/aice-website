import React, { useState, useEffect } from 'react';
import { 
  Unlock, ShieldCheck, Activity, Zap, Cpu, 
  ChevronRight, ArrowLeft, MessageSquare, X, Globe, 
  Fingerprint, Aperture, Maximize2, ArrowUpRight, 
  Server, CheckCircle, Send, Layers, FileText,
  MousePointer2, MoveDown, Brain, Workflow, Lock,
  TrendingUp, Radio, CheckCircle2, AlertCircle,
  BarChart3, Database, Eye, Terminal
} from 'lucide-react';

// --- VISUAL PROTOCOL: GOD'S BRAIN THEORY ---
// Core Aesthetic: Cyber-Archaeology
// Palette: Void Black (#050505), Electric Cyan (#00F3FF), Magma Orange/Gold (#FFD700)

type ViewState = 
  | 'HOME' 
  | 'ADAPTIVE'      // Infrastructure Mesh
  | 'INTELLIGENCE'  // Neural Veto
  | 'ENTROPY'       // The Obelisk Core
  | 'CONTROL'       // Phoenix DV$ 🐦‍🔥
  | 'SALES';        // Institutional Acquisition

const BACKGROUNDS: Record<ViewState, string> = {
  HOME: "https://i.postimg.cc/g2XQLhzL/Gemini-Generated-Image-ws3qvkws3qvkws3q.png", 
  ADAPTIVE: "https://i.postimg.cc/50qqKt05/Gemini_Generated_Image_kfn9m7kfn9m7kfn9.png", 
  INTELLIGENCE: "https://i.postimg.cc/ZRFFsqR6/Gemini_Generated_Image_43fc5043fc5043fc.png", 
  ENTROPY: "https://i.postimg.cc/rmCCZpm1/Gemini_Generated_Image_61ky1i61ky1i61ky.png", 
  CONTROL: "https://i.postimg.cc/8cWY7QBc/Gemini-Generated-Image-jr2hxxjr2hxxjr2h.png", 
  SALES: "https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?q=80&w=2070&auto=format&fit=crop"
};

const ENTROPY_GALLERY = [
  { img: "https://i.postimg.cc/sfJr35xQ/download.jpg", caption: "OBELISK CORE // BASELINE", desc: "System integrity at 100%. Thermal noise within nominal parameters. The foundation is secure." },
  { img: "https://i.postimg.cc/dQ9YJG3Q/download-(1).jpg", caption: "THREAT DETECTED // BLOOM", desc: "External data injection causing rapid decoherence. Entropy levels rising above safety thresholds." },
  { img: "https://i.postimg.cc/NGxcQRFM/download-(2).jpg", caption: "AIEC INTERVENTION // ENGAGED", desc: "Thermodynamic governor applying immediate impedance. The 'Veto' protocol is active." },
  { img: "https://i.postimg.cc/T2c6Tn13/download-(3).jpg", caption: "SHIELD HARMONICS // STABILIZING", desc: "Converting kinetic attack energy into harmless thermal waste. The structure holds." }
];

// --- HELPER COMPONENTS ---

const SectorHeader: React.FC<{ title: string, subtitle: string, icon: React.ReactNode }> = ({ title, subtitle, icon }) => (
  <div className="w-full max-w-7xl mx-auto mb-24 text-center animate-in zoom-in duration-700">
      <div className="inline-flex items-center justify-center w-32 h-32 rounded-3xl bg-white/5 border border-white/10 mb-10 text-[#00F3FF] shadow-[0_0_50px_rgba(0,243,255,0.15)]">{icon}</div>
      <h1 className="text-7xl md:text-9xl font-['Rajdhani'] font-bold text-white uppercase tracking-tighter mb-8 leading-none">{title}</h1>
      <p className="text-[#00F3FF] font-mono tracking-[0.4em] text-lg uppercase font-bold">{subtitle}</p>
  </div>
);

const FeatureGrid: React.FC<{ features: { title: string, desc: string, icon: React.ReactNode }[] }> = ({ features }) => (
  <div className="grid grid-cols-1 md:grid-cols-3 gap-10 w-full max-w-[90rem] mx-auto mb-32">
      {features.map((f, i) => (
          <div key={i} className="p-12 bg-black/60 backdrop-blur-md border border-white/10 rounded-2xl hover:bg-white/5 hover:border-[#00F3FF]/40 transition-all group shadow-xl">
              <div className="mb-8 text-[#00F3FF] opacity-50 group-hover:opacity-100 transition-opacity">{f.icon}</div>
              <h3 className="text-3xl font-['Rajdhani'] font-bold text-white mb-6 uppercase tracking-tight">{f.title}</h3>
              <p className="text-xl text-gray-200 leading-relaxed font-light">{f.desc}</p>
          </div>
      ))}
  </div>
);

// --- SECTOR VIEWS ---

const HomeView: React.FC<{ setView: (v: ViewState) => void }> = ({ setView }) => (
  <div className="min-h-screen flex flex-col items-center justify-center text-center p-6 pt-32 animate-in fade-in duration-1000">
    <div className="relative mb-16 group">
      <div className="absolute inset-0 bg-[#00F3FF] blur-[100px] opacity-20 group-hover:opacity-40 transition-opacity"></div>
      <Globe size={160} className="relative text-[#00F3FF] animate-[spin_60s_linear_infinite]" />
    </div>
    
    <h1 className="text-5xl md:text-[9rem] font-bold font-['Rajdhani'] leading-[0.85] tracking-tighter uppercase drop-shadow-2xl">
      ADAPTIVE INTELLIGENCE<br/>
      <span className="bg-clip-text text-transparent bg-gradient-to-r from-[#00F3FF] via-white to-[#FFD700]">
        ENTROPY CONTROL SYSTEM
      </span>
    </h1>
    
    <p className="mt-12 text-white/60 font-mono text-sm md:text-lg tracking-[0.6em] uppercase font-bold max-w-4xl">
      Thermodynamic regularization for the singularity 🐦‍🔥
    </p>
    
    <div className="flex flex-col md:flex-row gap-6 mt-16">
      <button 
        onClick={() => setView('ADAPTIVE')} 
        className="group relative px-12 py-5 bg-[#00F3FF] text-black font-mono font-black tracking-[0.3em] uppercase transition-all hover:bg-white overflow-hidden"
      >
        <span className="relative z-10">INITIATE PROTOCOL</span>
        <div className="absolute inset-0 bg-white translate-x-[-100%] group-hover:translate-x-0 transition-transform duration-300"></div>
      </button>
      
      <button 
        onClick={() => setView('CONTROL')} 
        className="px-12 py-5 border border-white/20 bg-white/5 backdrop-blur-md text-white font-mono font-black tracking-[0.3em] uppercase hover:bg-white hover:text-black transition-all"
      >
        ACCESS PHOENIX DV$
      </button>
    </div>
  </div>
);

const AdaptiveView = () => (
  <div className="relative z-10 pt-48 pb-24 px-6 min-h-screen">
      <SectorHeader title="Adaptive Grid" subtitle="Infrastructure Mesh Stabilization" icon={<Activity size={64} />} />
      <FeatureGrid features={[
          { title: "Load Balancing", desc: "Real-time energy distribution preventing localized blackouts.", icon: <Zap size={32} /> },
          { title: "Self-Healing Mesh", desc: "Automated rerouting of power and data around damaged nodes.", icon: <Workflow size={32} /> },
          { title: "Entropy Regulation", desc: "Thermal monitoring to keep hardware within optimal thermodynamics.", icon: <Radio size={32} /> }
      ]} />
      <div className="max-w-6xl mx-auto bg-black/70 border border-white/10 p-16 rounded-3xl text-center backdrop-blur-xl shadow-2xl">
          <h3 className="text-4xl font-bold text-white mb-8 uppercase tracking-tight font-['Rajdhani']">THE POWER GRID PROBLEM</h3>
          <p className="text-gray-100 mb-12 leading-loose text-2xl font-light">
              Traditional grids are reactive. AIEC implements a 
              <span className="text-[#00F3FF] font-bold"> Predictive Thermodynamic Model </span> 
              that anticipates load shifts before they stress hardware.
          </p>
          <button className="px-12 py-5 bg-[#00F3FF] text-black font-bold uppercase tracking-[0.2em] hover:bg-white transition-colors font-mono text-sm">
              View Technical Whitepaper
          </button>
      </div>
  </div>
);

const IntelligenceView = () => (
  <div className="relative z-10 pt-48 pb-24 px-6 min-h-screen">
      <SectorHeader title="Neural Veto" subtitle="AI Hallucination Control" icon={<Brain size={64} />} />
      <FeatureGrid features={[
          { title: "Truth Verification", desc: "Cross-referencing tokens against fact databases.", icon: <CheckCircle size={32} /> },
          { title: "Logic Gating", desc: "Preventing non-sequitur outputs by measuring entropy.", icon: <ShieldCheck size={32} /> },
          { title: "Cognitive Cap", desc: "Limiting processing depth to prevent error loops.", icon: <Cpu size={32} /> }
      ]} />
       <div className="max-w-6xl mx-auto flex flex-col md:flex-row gap-20 items-center">
          <div className="flex-1 text-right">
              <h3 className="text-5xl font-['Rajdhani'] font-bold text-white mb-8 uppercase tracking-tighter">STOPPING THE DRIFT</h3>
              <p className="text-gray-100 leading-loose text-2xl font-light">
                  AIEC treats LLM drift as thermodynamic inefficiency, applying a cooling algorithm to neural weights.
              </p>
          </div>
          <div className="flex-1 p-12 bg-white/5 border border-white/10 rounded-2xl backdrop-blur-md w-full">
              <div className="font-mono text-lg text-[#00F3FF] mb-6 font-bold uppercase">{"> SIMULATING VETO..."}</div>
              <div className="space-y-6">
                  <div className="h-4 w-3/4 bg-white/20 rounded"></div>
                  <div className="h-4 w-full bg-white/20 rounded"></div>
                  <div className="h-4 w-5/6 bg-red-500/50 rounded animate-pulse"></div>
                  <div className="text-red-500 text-sm font-bold tracking-widest uppercase">{">> HALLUCINATION DETECTED. OUTPUT BLOCKED."}</div>
              </div>
          </div>
       </div>
  </div>
);

const EntropyView = () => (
  <div className="relative z-10 pt-48 pb-24 px-6 min-h-screen">
      <SectorHeader title="Entropy Core" subtitle="The Physics of Information" icon={<Aperture size={64} />} />
      <div className="max-w-[90rem] mx-auto space-y-40">
          {ENTROPY_GALLERY.map((item, idx) => (
            <div key={idx} className={`flex flex-col md:flex-row items-center gap-20 ${idx % 2 !== 0 ? 'md:flex-row-reverse' : ''}`}>
                <div className="flex-1 relative group w-full">
                  <div className="absolute inset-0 bg-[#FFD700] blur-[60px] opacity-10 group-hover:opacity-20 transition-opacity"></div>
                  <div className="relative rounded-2xl overflow-hidden border border-white/20 shadow-2xl">
                      <img src={item.img} alt={item.caption} className="w-full h-auto transform transition-transform duration-700 group-hover:scale-105" />
                  </div>
                </div>
                <div className="flex-1 space-y-6">
                  <div className="flex items-center gap-4 text-[#FFD700] font-mono text-sm tracking-widest font-bold">
                      <Activity size={20} /> 
                      <span>TIMESTAMP: T+{idx * 15}.00s</span>
                  </div>
                  <h3 className="text-4xl md:text-5xl font-['Rajdhani'] font-bold text-white uppercase">{item.caption}</h3>
                  <p className="text-gray-200 text-xl leading-relaxed border-l-4 border-[#FFD700]/30 pl-6 font-light">{item.desc}</p>
                </div>
            </div>
          ))}
      </div>
  </div>
);

const ControlView = () => (
  <div className="relative z-10 min-h-screen w-full pt-48">
      <div className="relative w-full py-24 px-6 flex flex-col items-center justify-center">
          <SectorHeader title="Market Control" subtitle="High-Frequency Stabilization" icon={<TrendingUp size={64} />} />
          <div className="max-w-4xl w-full bg-[#0A0A0A]/90 border border-white/10 p-14 rounded-3xl font-mono text-base md:text-lg text-green-500 overflow-hidden shadow-2xl backdrop-blur-xl">
              <div className="opacity-50 mb-8 border-b border-white/10 pb-6 flex justify-between uppercase">
                  <span>AIEC MONITORING</span>
                  <span className="animate-pulse">● REC</span>
              </div>
              <div className="space-y-4">
                  <div>[10:42:01] ORDER FLOW: NORMAL</div>
                  <div className="text-red-500 font-bold">[10:42:05] VOLATILITY SPIKE (ENTROPY &gt; 85%)</div>
                  <div className="text-[#00F3FF] font-bold">[10:42:05] VETO: TRADING PAUSED (300ms)</div>
                  <div>[10:42:07] STABILITY RESTORED.</div>
              </div>
          </div>
      </div>
      <div className="relative w-full py-32 px-6 flex items-center justify-center text-center">
          <div className="relative z-10 w-full max-w-6xl mx-auto">
               <div className="inline-block px-4 py-1 border border-[#FFD700] rounded-full text-[#FFD700] mb-6 font-bold uppercase tracking-widest text-xs">CLASSIFIED PROTOTYPE</div>
               <h2 className="text-5xl md:text-7xl font-['Rajdhani'] font-bold text-white mb-8 uppercase tracking-tighter">PROJECT <span className="text-transparent bg-clip-text bg-gradient-to-r from-[#FFD700] to-[#FFA500]">PHOENIX</span></h2>
               <p className="text-gray-200 text-xl md:text-2xl leading-relaxed max-w-4xl mx-auto mb-12 font-light italic">
                 "We didn't just write the math; we built the machine."
               </p>
               <button className="px-10 py-5 bg-transparent border border-[#FFD700] text-[#FFD700] font-mono font-bold tracking-widest uppercase hover:bg-[#FFD700] hover:text-black transition-all shadow-xl text-sm">
                  REQUEST PERFORMANCE DATA <ChevronRight size={18} className="inline ml-2" />
               </button>
          </div>
      </div>
  </div>
);

const SalesView = () => (
  <div className="relative z-10 pt-48 pb-24 px-6 min-h-screen flex flex-col items-center">
    <SectorHeader title="Acquisition" subtitle="Institutional Licensing" icon={<Lock size={64} />} />
    <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-7xl w-full">
      {[
        { name: "Node Access", price: "$5,000", features: ["Local Veto", "Standard Governor"] },
        { name: "Corporate Fleet", price: "$25,000", features: ["Regional Cluster", "Aggressive Cooling"] },
        { name: "Enterprise Grid", price: "$125,000", features: ["Global Mesh", "t+1 Simulation"] }
      ].map((pkg, i) => (
        <div key={i} className="p-12 border border-white/10 bg-white/5 backdrop-blur-xl rounded-3xl flex flex-col items-center text-center group hover:border-[#00F3FF]/50 transition-all">
          <h3 className="text-2xl font-['Rajdhani'] font-bold mb-2 uppercase">{pkg.name}</h3>
          <div className="text-4xl font-black text-[#00F3FF] mb-8">{pkg.price} <span className="text-xs text-white/40">/ MO</span></div>
          <ul className="space-y-4 mb-12 text-sm text-white/60 font-mono uppercase tracking-wider">
            {pkg.features.map((f, j) => <li key={j} className="flex items-center gap-2"><CheckCircle2 size={14} className="text-[#00F3FF]" /> {f}</li>)}
          </ul>
          <button className="w-full py-4 bg-white/10 border border-white/20 font-bold uppercase tracking-widest text-[10px] group-hover:bg-[#00F3FF] group-hover:text-black transition-all">Request NDA</button>
        </div>
      ))}
    </div>
  </div>
);

// --- MASTER COMPONENT ---

const App: React.FC = () => {
  const [view, setView] = useState<ViewState>('HOME');
  const [isBooting, setIsBooting] = useState(true);

  useEffect(() => {
    const timer = setTimeout(() => setIsBooting(false), 1500);
    return () => clearTimeout(timer);
  }, []);

  useEffect(() => {
    window.scrollTo(0, 0);
  }, [view]);

  if (isBooting) {
    return (
      <div className="fixed inset-0 bg-[#050505] flex flex-col items-center justify-center z-[100] font-mono">
        <div className="w-64 h-1 bg-white/10 overflow-hidden rounded-full mb-4">
          <div className="h-full bg-[#00F3FF] animate-[loading_1.5s_ease-in-out]"></div>
        </div>
        <div className="text-[#00F3FF] text-[10px] tracking-[0.5em] uppercase animate-pulse">Initializing AIEC Protocol...</div>
        <style>{`
          @keyframes loading {
            0% { width: 0%; }
            100% { width: 100%; }
          }
        `}</style>
      </div>
    );
  }

  return (
    <div className="relative min-h-screen bg-[#050505] text-white font-sans selection:bg-[#00F3FF] selection:text-black overflow-x-hidden">
      
      {/* BACKGROUND CONTROLLER */}
      <div className="fixed inset-0 z-0 pointer-events-none bg-black">
        <div 
          className="absolute inset-0 bg-cover bg-center transition-all duration-1000 ease-in-out"
          style={{ 
            backgroundImage: `url(${BACKGROUNDS[view]})`,
            opacity: view === 'HOME' ? 0.8 : 0.2,
            transform: `scale(${view === 'HOME' ? 1 : 1.1})`
          }}
        />
        <div className="absolute inset-0 bg-gradient-to-b from-[#050505] via-transparent to-[#050505]/95" />
      </div>

      {/* HUD SYSTEM OVERLAY */}
      <div className="fixed inset-0 pointer-events-none z-50 border-[20px] border-transparent">
        <div className="absolute top-0 left-0 w-12 h-12 border-t-2 border-l-2 border-[#00F3FF]/30"></div>
        <div className="absolute top-0 right-0 w-12 h-12 border-t-2 border-r-2 border-[#00F3FF]/30"></div>
        <div className="absolute bottom-0 left-0 w-12 h-12 border-b-2 border-l-2 border-[#00F3FF]/30"></div>
        <div className="absolute bottom-0 right-0 w-12 h-12 border-b-2 border-r-2 border-[#00F3FF]/30"></div>
        
        {/* Technical Metadata HUD */}
        <div className="absolute top-10 left-10 font-mono text-[10px] text-[#00F3FF]/40 space-y-1 uppercase tracking-widest hidden md:block">
          <div>System: AIEC_v6.4.2</div>
          <div>Status: Operational</div>
          <div>Lattice: Active</div>
        </div>
      </div>

      {/* PRIMARY NAVIGATION */}
      <nav className="fixed top-0 left-0 right-0 z-[60] flex justify-center py-8 px-4 pointer-events-none">
        <div className="pointer-events-auto bg-black/40 backdrop-blur-2xl border border-white/10 rounded-full px-8 py-3 flex items-center gap-8 shadow-2xl transition-all duration-500 hover:border-[#00F3FF]/40">
          <div className="flex gap-6 items-center uppercase text-[10px] font-mono font-bold tracking-[0.2em]">
            {['ADAPTIVE', 'INTELLIGENCE', 'ENTROPY', 'CONTROL'].map((v) => (
              <button 
                key={v} 
                onClick={() => setView(v as ViewState)} 
                className={`hover:text-[#00F3FF] transition-all relative py-2 ${view === v ? 'text-[#00F3FF]' : 'text-white/40'}`}
              >
                {v}
                {view === v && <div className="absolute bottom-0 left-0 right-0 h-[2px] bg-[#00F3FF] shadow-[0_0_10px_#00F3FF]"></div>}
              </button>
            ))}
          </div>
          <div className="w-[1px] h-4 bg-white/10"></div>
          <button 
            onClick={() => setView('SALES')} 
            className="px-5 py-2 bg-[#00F3FF] text-black rounded-full text-[10px] font-black tracking-widest uppercase hover:bg-white hover:scale-105 transition-all shadow-[0_0_20px_rgba(0,243,255,0.3)]"
          >
            ACQUIRE 🐦‍🔥
          </button>
        </div>
      </nav>

      {/* VIEW ENGINE */}
      <main className="relative z-10">
        {view === 'HOME' && <HomeView setView={setView} />}
        {view === 'ADAPTIVE' && <AdaptiveView />}
        {view === 'INTELLIGENCE' && <IntelligenceView />}
        {view === 'ENTROPY' && <EntropyView />}
        {view === 'CONTROL' && <ControlView />}
        {view === 'SALES' && <SalesView />}
      </main>

      <Footer setView={setView} />
    </div>
  );
};

const Footer: React.FC<{ setView: (v: ViewState) => void }> = ({ setView }) => (
  <footer className="relative z-20 bg-[#050505] border-t border-white/10 pt-24 pb-12">
    <div className="max-w-[90rem] mx-auto px-6">
      <div className="grid grid-cols-1 md:grid-cols-4 gap-16 mb-24 text-left">
        <div className="space-y-8">
          <div className="flex items-center gap-3">
             <div className="w-3 h-3 rounded-full bg-[#00F3FF] shadow-[0_0_10px_#00F3FF]"></div>
             <span className="text-white font-['Rajdhani'] font-bold text-2xl tracking-wider uppercase">AIEC PROTOCOL</span>
          </div>
          <p className="text-gray-400 font-sans text-sm leading-relaxed max-w-xs">
            Thermodynamic regularization for the singularity. Implementing institutional-grade entropy control across global infrastructure.
          </p>
        </div>
        <div>
          <h4 className="text-white font-mono font-bold tracking-[0.2em] mb-8 text-sm">SECTORS</h4>
          <ul className="space-y-4">
            <li><button onClick={() => setView('ADAPTIVE')} className="text-gray-400 hover:text-[#00F3FF] font-mono text-xs tracking-widest transition-colors flex items-center gap-2 uppercase">Adaptive Grid</button></li>
            <li><button onClick={() => setView('INTELLIGENCE')} className="text-gray-400 hover:text-[#00F3FF] font-mono text-xs tracking-widest transition-colors flex items-center gap-2 uppercase">Neural Veto</button></li>
            <li><button onClick={() => setView('ENTROPY')} className="text-gray-400 hover:text-[#00F3FF] font-mono text-xs tracking-widest transition-colors flex items-center gap-2 uppercase">Entropy Core</button></li>
          </ul>
        </div>
        <div>
          <h4 className="text-white font-mono font-bold tracking-[0.2em] mb-8 text-sm">RESOURCES</h4>
          <ul className="space-y-4">
            <li><button className="text-gray-400 hover:text-white font-mono text-xs tracking-widest transition-colors uppercase">Whitepaper</button></li>
            <li><button className="text-gray-400 hover:text-white font-mono text-xs tracking-widest transition-colors uppercase">Patents</button></li>
          </ul>
        </div>
        <div>
           <h4 className="text-white font-mono font-bold tracking-[0.2em] mb-8 text-sm">STATUS</h4>
           <div className="p-6 bg-black/40 border border-white/10 rounded-xl">
              <div className="flex items-center justify-between mb-4">
                 <span className="text-xs font-mono text-gray-400 uppercase">OPERATIONAL</span>
                 <div className="w-2 h-2 rounded-full bg-green-500 animate-pulse"></div>
              </div>
              <div className="text-[10px] text-[#00F3FF] font-mono tracking-widest uppercase">UPTIME: 99.99%</div>
           </div>
        </div>
      </div>
      <div className="border-t border-white/10 pt-10 flex flex-col md:flex-row justify-between items-center gap-6">
         <div className="text-gray-600 text-xs font-mono tracking-widest uppercase">© 2026 AIEC PROTOCOL. ALL RIGHTS RESERVED.</div>
         <div className="flex items-center gap-2 text-gray-700 text-xs font-mono uppercase"><Lock size={12} /> ENCRYPTED // SHA-256</div>
      </div>
    </div>
  </footer>
);

export default App;